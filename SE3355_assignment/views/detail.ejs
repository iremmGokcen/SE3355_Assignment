<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ürün Detayı</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <%- include('navbar') %>
    <div class="container mt-4">
        <h1><%= product.description %></h1>
        <img src="/images/<%= product.image %>" class="img-fluid responsive-image mb-4" alt="<%= product.description %>">
        <p><strong>Fiyat:</strong> <%= product.price %> TL</p>
        <p><strong>Şehir:</strong> <%= product.city %></p>
        <p><strong>Kategori:</strong> <%= product.category %></p>
        <p><strong>İlan Numarası:</strong> <%= product.ad_no %></p>
        <a href="/" class="btn btn-secondary">Anasayfaya Dön</a>
    </div>
    <script src="/script.js"></script>
</body>
</html>
